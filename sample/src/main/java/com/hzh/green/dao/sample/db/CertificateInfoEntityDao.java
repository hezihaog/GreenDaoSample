package com.hzh.green.dao.sample.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.hzh.green.dao.sample.model.entity.CertificateInfoEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "certificate_info".
*/
public class CertificateInfoEntityDao extends AbstractDao<CertificateInfoEntity, String> {

    public static final String TABLENAME = "certificate_info";

    /**
     * Properties of entity CertificateInfoEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, String.class, "id", true, "ID");
        public final static Property CreateTime = new Property(1, java.util.Date.class, "createTime", false, "create_time");
        public final static Property UpdateTime = new Property(2, java.util.Date.class, "updateTime", false, "update_time");
        public final static Property DeleteFlag = new Property(3, Integer.class, "deleteFlag", false, "delete_flag");
        public final static Property Version = new Property(4, Integer.class, "version", false, "version");
        public final static Property PersonInfoId = new Property(5, String.class, "personInfoId", false, "PERSON_INFO_ID");
        public final static Property CertificateType = new Property(6, String.class, "certificateType", false, "certificate_type");
        public final static Property CertificateNo = new Property(7, String.class, "certificateNo", false, "certificate_no");
    }


    public CertificateInfoEntityDao(DaoConfig config) {
        super(config);
    }
    
    public CertificateInfoEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"certificate_info\" (" + //
                "\"ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: id
                "\"create_time\" INTEGER NOT NULL ," + // 1: createTime
                "\"update_time\" INTEGER NOT NULL ," + // 2: updateTime
                "\"delete_flag\" INTEGER NOT NULL ," + // 3: deleteFlag
                "\"version\" INTEGER NOT NULL ," + // 4: version
                "\"PERSON_INFO_ID\" TEXT," + // 5: personInfoId
                "\"certificate_type\" TEXT NOT NULL ," + // 6: certificateType
                "\"certificate_no\" TEXT NOT NULL );"); // 7: certificateNo
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"certificate_info\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CertificateInfoEntity entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
        stmt.bindLong(2, entity.getCreateTime().getTime());
        stmt.bindLong(3, entity.getUpdateTime().getTime());
        stmt.bindLong(4, entity.getDeleteFlag());
        stmt.bindLong(5, entity.getVersion());
 
        String personInfoId = entity.getPersonInfoId();
        if (personInfoId != null) {
            stmt.bindString(6, personInfoId);
        }
        stmt.bindString(7, entity.getCertificateType());
        stmt.bindString(8, entity.getCertificateNo());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CertificateInfoEntity entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
        stmt.bindLong(2, entity.getCreateTime().getTime());
        stmt.bindLong(3, entity.getUpdateTime().getTime());
        stmt.bindLong(4, entity.getDeleteFlag());
        stmt.bindLong(5, entity.getVersion());
 
        String personInfoId = entity.getPersonInfoId();
        if (personInfoId != null) {
            stmt.bindString(6, personInfoId);
        }
        stmt.bindString(7, entity.getCertificateType());
        stmt.bindString(8, entity.getCertificateNo());
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public CertificateInfoEntity readEntity(Cursor cursor, int offset) {
        CertificateInfoEntity entity = new CertificateInfoEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // id
            new java.util.Date(cursor.getLong(offset + 1)), // createTime
            new java.util.Date(cursor.getLong(offset + 2)), // updateTime
            cursor.getInt(offset + 3), // deleteFlag
            cursor.getInt(offset + 4), // version
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // personInfoId
            cursor.getString(offset + 6), // certificateType
            cursor.getString(offset + 7) // certificateNo
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CertificateInfoEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setCreateTime(new java.util.Date(cursor.getLong(offset + 1)));
        entity.setUpdateTime(new java.util.Date(cursor.getLong(offset + 2)));
        entity.setDeleteFlag(cursor.getInt(offset + 3));
        entity.setVersion(cursor.getInt(offset + 4));
        entity.setPersonInfoId(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setCertificateType(cursor.getString(offset + 6));
        entity.setCertificateNo(cursor.getString(offset + 7));
     }
    
    @Override
    protected final String updateKeyAfterInsert(CertificateInfoEntity entity, long rowId) {
        return entity.getId();
    }
    
    @Override
    public String getKey(CertificateInfoEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CertificateInfoEntity entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
